# Arm B: frozen V-JEPA 2.1 encoder probe (spec §6.B/C, §13).
# Loads the local vjepa2 submodule (models/vjepa2) + checkpoint
# vjepa2_1_vitb_dist_vitG_384 (1.6 GB, first run: submodule update + weights).
arm: vjepa_encoder
frozen: true
