# Arm B: frozen V-JEPA 2.1 encoder-only probe (spec §6.B, §13).
# PIN the exact Hugging Face checkpoint id and package versions.
arm: vjepa_encoder
checkpoint: facebook/vjepa2-<pin-exact-id>
frozen: true
embedding_dim: 768
