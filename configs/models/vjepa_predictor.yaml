# Arm C: frozen V-JEPA 2.1 encoder + predictor (spec §6.C, §13).
# obs_fraction: fraction of temporal tokens treated as observed context; the
# rest are predicted by the frozen predictor (teacher space, 1664 dim).
arm: vjepa_predictor
frozen: true
obs_fraction: 0.6
predictor_horizon_delta: 60
