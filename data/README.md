# Data directory (spec §4)
#
# Raw datasets live under data/raw/ and are NEVER committed (licensing).
# Normalized artifacts:
#   manifests/   JSONL tracklet manifests (schema in spec §4.3)
#   gap_trials/  JSONL disappearance-gap trials (schema in spec §4.4)
